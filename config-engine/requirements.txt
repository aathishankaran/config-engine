# Config Engine (Flask web UI)
# Python 3.6.8  |  Java 11.0.25  |  Linux / macOS / Windows

# ── Web framework ──────────────────────────────────────────────────────────────
# Flask 2.0.x is the last series supporting Python 3.6; Flask 2.1+ requires 3.7+
flask>=2.0,<2.1
werkzeug>=2.0,<2.1

# ── Data validation & serialisation ───────────────────────────────────────────
# pydantic 2.x requires Python 3.7+; v1 supports Python 3.6
pydantic>=1.0,<2.0

# ── Data processing (schema parsing, test data preview) ───────────────────────
# pandas 1.2+ requires Python 3.7.1+; 1.1.x is the last release supporting 3.6
pandas>=1.1.0,<1.2
# pyarrow 6.0+ requires Python 3.7+; 4.x/5.x support Python 3.6
pyarrow>=4.0.0,<6.0

# ── COBOL copybook parsing (server-side schema import) ────────────────────────
# ebcdic>=1.1.1           # optional: EBCDIC encoding/decoding for COBOL files

# ── Dev extras ────────────────────────────────────────────────────────────────
# pytest>=4.0,<5.0       # pytest 5+ requires Python 3.5+; 4.6.x is last for 3.6
# httpx                  # not available for Python 3.6; use requests instead
